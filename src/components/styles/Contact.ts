import styled from 'styled-components';

export const ContactStyles = styled.section`
  padding: 20px;

  h1 {
    font-size: 1.4rem;
    margin: 10px 0;
    text-align: center;
  }
  form {
    label {
      color: #fff;
    }
    textarea {
      resize: none;
    }
  }
`;
